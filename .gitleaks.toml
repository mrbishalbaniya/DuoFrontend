title = "Duo Frontend gitleaks"

[allowlist]
description = "Template env files contain placeholder values, not production secrets"
paths = [
  '''(?:^|/)\.env\.example$''',
  '''(?:^|/)\.env\.local\.example$''',
]
